# Widget bundles

Generated by `earth-dynamics` — **do not hand-edit** files under `weatherly/`.

```bash
# From earth-dynamics repo:
npm run build:widget
node scripts/sync-widget-to-eds.mjs
```

Or merge the automated PR from `.github/workflows/sync-widget-eds.yml` in `somarc/earth-dynamics`.